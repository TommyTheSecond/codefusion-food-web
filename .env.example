# Base URL of the AI backend (the FastAPI service).
# Local dev (backend running via `uvicorn api:app --port 8000`):
VITE_API_URL=http://localhost:8000
# When deployed, set this to your hosted API, e.g.:
# VITE_API_URL=https://restaurant-demand-forecasting.onrender.com
